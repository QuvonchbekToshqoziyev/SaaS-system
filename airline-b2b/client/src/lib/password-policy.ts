export const MIN_PASSWORD_LENGTH = 12;

export const passwordLengthMessage = {
  en: `Password must be at least ${MIN_PASSWORD_LENGTH} characters`,
  uz: `Parol kamida ${MIN_PASSWORD_LENGTH} ta belgidan iborat bo‘lishi kerak`,
};
